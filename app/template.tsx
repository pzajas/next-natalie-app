import { PageRouteShell } from "@/components/natalie/PageRouteShell";

export default function Template({ children }: { children: React.ReactNode }) {
  return <PageRouteShell>{children}</PageRouteShell>;
}
